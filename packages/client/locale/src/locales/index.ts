/**
 * The common-namespace dictionary. English is the only shipped locale, so it
 * is the key-set source of truth; `CommonKey` is its key union.
 */
export { en } from './en.ts'
export type { CommonKey } from './en.ts'
