# Per-launch SDK customization: retain only persistent Bash and the string
# editor as model-facing tools, with process-local providers and an explicit
# danger-full-access / never-ask policy.

- insert:
    - id: tool-str-replace-editor
      name: '@akashx/akx-tool-str-replace-editor'
      config:
        maxOutputChars: 16000

- id: llm-akx
  name: '@akashx/akx-llm-akx'
  config:
    apiKeyEnv: AKASHX_API_KEY
    streamIdleTimeoutMs: 172800000
    models:
      - id: !!js process.env.AKX_MODEL ?? 'akashx-v4-flash'
        contextWindow: !!js Number(process.env.AKX_CONTEXT_WINDOW ?? 1000000)

- id: sandbox-policy
  name: '@akashx/akx-sandbox-policy'
  config:
    mode: danger-full-access
    workspaceRoot: !!js process.cwd()

- id: approval
  name: '@akashx/akx-user-approval'
  config:
    policy: never

- id: permission
  name: '@akashx/akx-permission-presets'
  disabled: true

- id: bash-sandbox
  name: '@akashx/akx-bash-sandbox'
  disabled: true

- id: pwsh-sandbox
  name: '@akashx/akx-pwsh-sandbox'
  disabled: true

- id: fs-sandbox
  name: '@akashx/akx-fs-sandbox'
  disabled: true

- id: tool-bash
  name: '@akashx/akx-tool-bash'
  disabled: true

- id: tool-pwsh
  name: '@akashx/akx-tool-pwsh'
  disabled: true

- id: agent-instructions
  name: '@akashx/akx-agent-instructions'
  disabled: true

- id: skill
  name: '@akashx/akx-skill'
  disabled: true

- id: skill-filesystem
  name: '@akashx/akx-skill-filesystem'
  disabled: true

- id: tool-skill
  name: '@akashx/akx-tool-skill'
  disabled: true

- id: tool-jobs
  name: '@akashx/akx-tool-jobs'
  disabled: true

- id: tool-fs
  name: '@akashx/akx-tool-fs'
  disabled: true

- id: tool-fs-search
  name: '@akashx/akx-tool-fs-search'
  disabled: true

- id: tool-subagent-control
  name: '@akashx/akx-tool-subagent-control'
  disabled: true

- id: tool-subagent-list-agents
  name: '@akashx/akx-tool-subagent-control/list-agents'
  disabled: true

- id: tool-subagent
  name: '@akashx/akx-tool-subagent'
  disabled: true

- id: tool-subagent-fork
  name: '@akashx/akx-tool-subagent'
  disabled: true

- id: tool-workflow
  name: '@akashx/akx-tool-workflow'
  disabled: true

- id: tool-todo
  name: '@akashx/akx-tool-todo'
  disabled: true

- id: tool-goal
  name: '@akashx/akx-tool-goal'
  disabled: true

- id: tool-ralph
  name: '@akashx/akx-tool-ralph'
  disabled: true

- id: tool-web
  name: '@akashx/akx-tool-web'
  disabled: true

- id: plan-mode
  name: '@akashx/akx-plan-mode'
  disabled: true

- id: system-prompt
  name: '@akashx/akx-system-prompt'
  config:
    includeHarnessIdentity: false
    personaPrefix: !!js process.env.AKX_SYSTEM_PROMPT ?? 'You are a helpful software engineer assistant.'

- id: session-persistence-jsonl
  name: '@akashx/akx-session-persistence-jsonl'
  config:
    root: !!js akxHomePath('sessions')
    compression: none

- insert:
    - id: bash-local
      name: '@akashx/akx-bash-local'

    - id: fs-local
      name: '@akashx/akx-fs-local'
      config:
        cwd: !!js process.cwd()

    - id: terminal
      name: '@akashx/akx-terminal'

    - id: terminal-bash
      name: '@akashx/akx-terminal-bash'
      config:
        timeoutMs: 300000

    - id: persistent-bash
      name: '@akashx/akx-tool-bash-persistent'
      config:
        timeoutMs: 300000
        description: |-
          Run commands in a bash shell
          * When invoking this tool, the contents of the "command" parameter does NOT need to be XML-escaped.
          * You don't have access to the internet via this tool.
          * You do have access to a mirror of common linux and python packages via apt and pip.
          * State is persistent across command calls and discussions with the user.
          * To inspect a particular line range of a file, e.g. lines 10-25, try 'sed -n 10,25p /path/to/the/file'.
          * Please avoid commands that may produce a very large amount of output.
          * Please run long lived commands in the background, e.g. 'sleep 10 &' or start a server in the background.
