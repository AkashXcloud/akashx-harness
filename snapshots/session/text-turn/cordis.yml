# Live-recording patch shared by the ordinary headless snapshot composition.
# The shipped profile owns application startup; this patch fixes only the
# deterministic test composition and raw persistence needed for recording.

- id: llm-akx
  name: '@akashx/akx-llm-akx'
  config:
    thinking: enabled
    reasoningEffort: max
    models:
      - id: akashx-v4-flash
      - id: akashx-v4-pro
      - id: akashx-v4-flash-vision-exp
        inputModalities: [text, image]

- id: sandbox-policy
  name: '@akashx/akx-sandbox-policy'
  config:
    mode: !!js "process.env.AKX_PERMISSION_MODE ?? (process.env.AKX_SNAPSHOT === undefined ? 'workspace-write' : 'danger-full-access')"
    workspaceRoot: !!js process.cwd()

- id: approval
  name: '@akashx/akx-user-approval'
  config:
    policy: !!js "(process.env.AKX_PERMISSION_MODE ?? (process.env.AKX_SNAPSHOT === undefined ? 'workspace-write' : 'danger-full-access')) === 'danger-full-access' ? 'never' : 'ask'"

- id: session-persistence-jsonl
  name: '@akashx/akx-session-persistence-jsonl'
  config:
    root: !!js akxHomePath('sessions')
    compression: none

- id: session-title-llm
  disabled: true

- id: system-prompt
  name: '@akashx/akx-system-prompt'
  config:
    personaPrefix: |
      You are a coding assistant powered by the {{model}} model. Your working directory is {{cwd}}. Your bash tool runs under a file sandbox — a `[sandbox: file access denied …]` result is policy, not a command bug.

      Verify your work by running the code or tests. Keep answers brief and factual.

- id: agent-instructions
  name: '@akashx/akx-agent-instructions'
  config:
    maxBytes: 65536

- id: tool-subagent
  name: '@akashx/akx-tool-subagent'
  config:
    provider: spawn
    toolName: subagent
    backgroundMode: continuable
    maxDepth: 1

- id: tool-subagent-fork
  name: '@akashx/akx-tool-subagent'
  config:
    provider: fork
    toolName: subagent_fork
    backgroundMode: one-shot
    enableRunInBackground: false
    maxDepth: 1

- id: fs-sandbox
  name: '@akashx/akx-fs-sandbox'
  config:
    cwd: !!js process.cwd()

- insert:
    - id: hooks-claude-code
      name: '@akashx/akx-hooks-claude-code'
      config:
        configPath: ./hooks.json

    - id: hooks-codex
      name: '@akashx/akx-hooks-codex'
      config:
        configPath: ./codex-hooks.json
